{
  "name": "profile-metrics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal, metrics-style GitHub profile SVG generator (plugin-based).",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
